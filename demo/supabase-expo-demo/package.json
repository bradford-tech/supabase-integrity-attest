{
  "name": "supabase-expo-demo",
  "version": "1.0.0",
  "private": true,
  "main": "index.ts",
  "scripts": {
    "check": "npm run format:check && npm run lint:check && npm run typecheck",
    "fix": "npm run format && npm run lint && npm run typecheck",
    "format": "prettier --write --cache --log-level warn \"**/*.{json,md,css,yml,yaml,html}\" && deno fmt --config supabase/functions/deno.json supabase/functions/ --quiet",
    "format:check": "prettier --check --cache --log-level warn \"**/*.{json,md,css,yml,yaml,html}\" && deno fmt --check --config supabase/functions/deno.json supabase/functions/ --quiet",
    "ios": "expo run:ios",
    "iphone": "expo run:ios --device --no-build-cache",
    "lint": "expo lint --fix . && for dir in supabase/functions/*/; do [ -f \"${dir}index.ts\" ] && (cd \"$dir\" && deno lint --fix --quiet index.ts); done",
    "lint:check": "expo lint . && for dir in supabase/functions/*/; do [ -f \"${dir}index.ts\" ] && (cd \"$dir\" && deno lint --quiet index.ts); done",
    "start": "expo start",
    "typecheck": "tsc --noEmit && for dir in supabase/functions/*/; do [ -f \"${dir}index.ts\" ] && (cd \"$dir\" && deno check --quiet index.ts); done",
    "web": "expo start --web"
  },
  "dependencies": {
    "@expo/app-integrity": "^55.0.13",
    "expo": "^57.0.9",
    "expo-constants": "~57.0.8",
    "expo-secure-store": "~57.0.1",
    "expo-splash-screen": "~57.0.5",
    "expo-status-bar": "~57.0.1",
    "react": "19.2.3",
    "react-native": "0.86.2",
    "react-native-safe-area-context": "~5.7.0"
  },
  "devDependencies": {
    "@eslint/compat": "^2.1.0",
    "@types/react": "~19.2.2",
    "eslint-config-expo": "~57.0.1",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.6",
    "supabase": "^2.111.0"
  }
}
